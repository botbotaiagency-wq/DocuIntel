## Packages
date-fns | Formatting dates in tables and headers

## Notes
- Using the pre-installed Replit Auth integration for authentication
- Using the pre-installed Object Storage hook (`useUpload`) for file uploads
- All shadcn UI components are assumed to be generated and available
